locals {
  # ── Project Identity ────────────────────────────────────────────────────────
  project_name   = "aws-eks-test2"
  ticket         = "PROJ-002"
  team           = "platform"

  # ── AWS ─────────────────────────────────────────────────────────────────────
  aws_account_id = "444455556666"
  region         = "eu-west-1"
  environment    = "staging"

  # ── Networking ──────────────────────────────────────────────────────────────
  vpc_cidr        = "10.0.2.0/24"
  private_subnets = ["10.0.2.0/26", "10.0.2.64/26"]
  public_subnets  = ["10.0.2.128/27", "10.0.2.160/27"]

  # ── EKS ─────────────────────────────────────────────────────────────────────
  eks_version            = "1.35"
  eks_node_instance_type = "m5.large"
}
