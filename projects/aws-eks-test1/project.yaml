locals {
  # ── Project Identity ────────────────────────────────────────────────────────
  project_name   = "aws-eks-test1"
  ticket         = "PROJ-001"
  team           = "platform"

  # ── AWS ─────────────────────────────────────────────────────────────────────
  aws_account_id = "111122223333"
  region         = "us-east-1"
  environment    = "dev"

  # ── Networking ──────────────────────────────────────────────────────────────
  vpc_cidr        = "10.0.1.0/24"
  private_subnets = ["10.0.1.0/26", "10.0.1.64/26"]
  public_subnets  = ["10.0.1.128/27", "10.0.1.160/27"]

  # ── EKS ─────────────────────────────────────────────────────────────────────
  eks_version            = "1.35"
  eks_node_instance_type = "m5.xlarge"
}
